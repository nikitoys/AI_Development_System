| Блок                                   | Статус | Что можно собрать сейчас                                                                                                                                                 |
| -------------------------------------- | -----: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **0. Metadata**                        |     🟡 | `Task`, `Reference`, `Status`, `Revision`, `Verification` — да. `Profile` сейчас не хранится и не выбирается командой, поэтому для MVP только default `execute`.         |
| **1. Role**                            |     🟡 | Можно брать `task.active_role`, fallback `Codex Executor`. Полноценная смена role по `profile` требует profile map.                                                      |
| **2. Objective**                       |      ✅ | `task.summary`, fallback `task.title`; рендерить одной короткой строкой.                                                                                                 |
| **3. Task Input**                      |      ✅ | `Task`, `Summary` есть.                                                                                                                                                  |
| **4. Scope**                           |      ✅ | Рендерить весь `task.scope`. Не делить на `<MAIN_ACTION>` / `<IMPORTANT_RELATED_ACTION>` / `<PRESERVE>`.                                                                 |
| **5. Out of Scope**                    |      ✅ | Брать `task.out_of_scope`; если пусто — compiler default.                                                                                                                |
| **6. Allowed Files**                   |     🟡 | `Editable` есть из `task.allowed_files`. `Read-only refs` — только если есть Context Pack.                                                                               |
| **7. Execution Steps**                 |      ❌ | Сейчас `task.execution_steps` нет. Блок не рендерить, пока поле не добавлено.                                                                                            |
| **8. Acceptance Criteria**             |      ✅ | Рендерить весь `task.acceptance_criteria`. Не делить на “what works / must not break / verify”.                                                                          |
| **9. Verification**                    |     🟡 | `Mode` есть из `task.verification_mode`. Стабильного поля для `<CHECK_COMMAND>` пока нет; для MVP рендерить default-инструкцию или позже добавить `verification_checks`. |
| **10. Context Manifest**               |     🟡 | Можно собрать, если Context Pack уже построен/подключён. Иначе `No Context Pack attached`.                                                                               |
| **11. Execution Rules / Missing Info** |      ✅ | Не из Task. Берётся из compiler policy/default.                                                                                                                          |
| **12. Final Report Format**            |      ✅ | Не из Task. Берётся из compiler profile/default.                                                                                                                         |
